<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Integra7AuralAlchemist.ViewModels"
             xmlns:controls="using:Integra7AuralAlchemist.Controls"
             x:DataType="vm:SNSynthToneEditorViewModel"
             x:CompileBindings="True"
             x:Name="Root"
             x:Class="Integra7AuralAlchemist.Views.SNSynthToneEditorView">

    <UserControl.Styles>
        <!-- Reserve a uniform 2-line height for control labels so wrapped labels don't push their
             slider/combo down — keeps every control in a row vertically aligned. -->
        <Style Selector="TextBlock.sliderLabel">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
        <Style Selector="ToggleButton.solo:checked">
            <Setter Property="Background" Value="{StaticResource SnSoloBrush}"/>
        </Style>
        <Style Selector="ToggleButton.mute:checked">
            <Setter Property="Background" Value="{StaticResource SnMuteBrush}"/>
        </Style>
    </UserControl.Styles>

    <Grid RowDefinitions="Auto,*" Margin="8">

        <!-- ===== Tone Header ===== -->
        <Border Grid.Row="0" Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
            <!-- Horizontal scrolling stays OFF: a ScrollViewer that scrolls horizontally measures its
                 content with unbounded width, so the WrapPanels below would never see a limit to wrap
                 at and would lay out on one endless line.

                 Vertical scrolling is ON with a ceiling. Wrapping makes the header grow on a narrow
                 window, and this row is Auto-sized, so without a cap it grows until it fills the
                 window and pushes the rack, partial card and tabs off the bottom. Capped, it wraps
                 freely up to the ceiling and scrolls beyond it, and the editor below keeps its
                 space. At a normal width the content is shorter than the cap, so no bar appears. -->
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                          MaxHeight="300">

            <StackPanel Spacing="8">
              <WrapPanel Orientation="Horizontal" ItemSpacing="6" LineSpacing="8">
                <StackPanel Spacing="1">
                    <Border Background="{StaticResource SnAccentBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                        <TextBlock Text="SuperNATURAL Synth" Foreground="{StaticResource SnBadgeForegroundBrush}" FontSize="11"/>
                    </Border>
                    <TextBlock Text="{Binding Header.ToneName}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Spacing="1" Width="160">
                    <TextBlock Text="Tone Level" />
                    <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding Header.ToneLevel.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                </StackPanel>

                <StackPanel Spacing="1">
                    <TextBlock Text="Voice" ToolTip.Tip="Mono Switch"/>
                    <ToggleSwitch OnContent="Mono" OffContent="Poly" IsChecked="{Binding Header.IsMono.Value, Mode=TwoWay}"/>
                    <ToggleSwitch OnContent="Legato" OffContent="Legato"
                                  IsChecked="{Binding Header.Legato.Value, Mode=TwoWay}"
                                  IsEnabled="{Binding Header.IsMono.Value}"
                                  ToolTip.Tip="Legato only applies in Mono mode."/>
                </StackPanel>

                <StackPanel Spacing="1" Width="150">
                    <TextBlock Text="Glide" ToolTip.Tip="Portamento"/>
                    <ToggleSwitch OnContent="On" OffContent="Off" IsChecked="{Binding Header.Portamento.Value, Mode=TwoWay}"/>
                    <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding Header.PortamentoTime.Value, Mode=TwoWay}"
                            IsEnabled="{Binding Header.Portamento.Value}"
                            ToolTip.Tip="Glide time (active only when Glide is on)." AccentBrush="{StaticResource KnobPitchDriftBrush}"/>
                </StackPanel>

                <StackPanel Spacing="1">
                    <TextBlock Text="Unison"/>
                    <ToggleSwitch OnContent="On" OffContent="Off" IsChecked="{Binding Header.Unison.Value, Mode=TwoWay}"/>
                    <ComboBox ItemsSource="{Binding Header.UnisonSize.Options}"
                              SelectedItem="{Binding Header.UnisonSize.Value, Mode=TwoWay}"
                              IsEnabled="{Binding Header.Unison.Value}"
                              ToolTip.Tip="Unison voice count (active only when Unison is on)."/>
                </StackPanel>

                <StackPanel Spacing="1" Width="150">
                    <TextBlock Text="Vintage Drift" ToolTip.Tip="Analog Feel"/>
                    <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding Header.AnalogFeel.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                </StackPanel>

                <StackPanel Spacing="1">
                    <TextBlock Text="Metallic Ring Mod" ToolTip.Tip="Ring Switch"/>
                    <ComboBox ItemsSource="{Binding Header.Ring.Options}"
                              SelectedItem="{Binding Header.Ring.Value, Mode=TwoWay}"/>
                </StackPanel>
                <Button VerticalAlignment="Bottom" Content="Advanced common parameters…"
                        Command="{Binding AdvancedCommonCommand}"/>
              </WrapPanel>

              <!-- Phrase (arpeggio) on its own row -->
              <WrapPanel Orientation="Horizontal" ItemSpacing="6" LineSpacing="8">
                <StackPanel Spacing="1" Width="260">
                    <TextBlock Text="Phrase (arpeggio)" ToolTip.Tip="Phrase Number"/>
                    <ComboBox MaxDropDownHeight="300" HorizontalAlignment="Stretch"
                              ItemsSource="{Binding Header.PhraseNumber.Options}"
                              SelectedItem="{Binding Header.PhraseNumber.Value, Mode=TwoWay}"/>
                </StackPanel>
                <StackPanel Spacing="1">
                    <TextBlock Text="Phrase Octave" ToolTip.Tip="Phrase Octave Shift"/>
                    <controls:RotaryKnob Minimum="-3" Maximum="3"
                                   Value="{Binding Header.PhraseOctaveShift.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchDriftBrush}"/>
                </StackPanel>
              </WrapPanel>
            </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ===== Rack + Selected partial ===== -->
        <!-- Scrolls sideways when the window is too narrow for the rack, the partial list and a
             usable parameter editor. MinWidth keeps the grid filling the window while it fits, so the
             star column still stretches and no bar appears; below that the grid keeps its natural
             width and scrolls, instead of squeezing the editor down to nothing. -->
        <ScrollViewer Grid.Row="1" x:Name="RackScroll" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">
        <Grid MinWidth="{Binding #RackScroll.Viewport.Width}">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="264"/>
            <!-- Never squeezed below a usable width; the grid scrolls sideways instead. -->
            <ColumnDefinition Width="*" MinWidth="420"/>
          </Grid.ColumnDefinitions>


            <ContentControl Grid.Column="0" Content="{Binding NoteRail}" Margin="0,0,8,0"/>

            <Grid Grid.Column="1" Margin="0,0,8,0" RowDefinitions="Auto,*">
              <Border Grid.Row="0" IsVisible="{Binding IsAuditioning}" Margin="0,0,0,8"
                      Background="{StaticResource SnAuditionBannerBrush}" CornerRadius="6" Padding="8">
                  <StackPanel Spacing="6">
                      <TextBlock Text="Auditioning (solo/mute) — restore before saving"
                                 TextWrapping="Wrap" FontSize="12"/>
                      <Button Content="Restore" HorizontalAlignment="Left" Command="{Binding ClearAuditionCommand}"/>
                  </StackPanel>
              </Border>
              <ListBox Grid.Row="1"
                     ItemsSource="{Binding Partials}"
                     SelectedItem="{Binding SelectedPartial, Mode=TwoWay}">
                <ListBox.Styles>
                    <!-- The card Border carries the selection colour; suppress the theme's faint
                         item highlight (almost invisible on the dark cards). -->
                    <Style Selector="ListBoxItem">
                        <Setter Property="Padding" Value="2"/>
                    </Style>
                    <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                    <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                    <!-- Default card look. Set via a Style (NOT a local Background on the Border) so the
                         :selected / :pointerover styles below can override it. -->
                    <Style Selector="ListBoxItem Border#CardRoot">
                        <Setter Property="Background" Value="{StaticResource SnCardBackgroundBrush}"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                    </Style>
                    <Style Selector="ListBoxItem:pointerover Border#CardRoot">
                        <Setter Property="Background" Value="{StaticResource SnCardHoverBackgroundBrush}"/>
                    </Style>
                    <Style Selector="ListBoxItem:selected Border#CardRoot">
                        <Setter Property="Background" Value="{StaticResource SnCardSelectedBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource SnCardSelectedBorderBrush}"/>
                    </Style>
                </ListBox.Styles>
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:SNSPartialViewModel">
                        <Border x:Name="CardRoot" Padding="8" CornerRadius="6">
                            <StackPanel Spacing="4">
                                <DockPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="4"
                                                VerticalAlignment="Center">
                                        <ToggleButton Classes="solo" Content="S" MinWidth="26" Width="26" Padding="0"
                                                      FontSize="11" IsChecked="{Binding Solo, Mode=TwoWay}"
                                                      ToolTip.Tip="Solo (audition)"/>
                                        <ToggleButton Classes="mute" Content="M" MinWidth="26" Width="26" Padding="0"
                                                      FontSize="11" IsChecked="{Binding Mute, Mode=TwoWay}"
                                                      ToolTip.Tip="Mute (audition)"/>
                                        <ToggleSwitch OnContent="" OffContent=""
                                                      IsChecked="{Binding Enabled, Mode=TwoWay}"
                                                      IsEnabled="{Binding !#Root.((vm:SNSynthToneEditorViewModel)DataContext).IsAuditioning}"
                                                      ToolTip.Tip="Partial on/off (disabled while auditioning)"/>
                                    </StackPanel>
                                </DockPanel>
                                <TextBlock Text="{Binding WaveSummary}" Opacity="0.8"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Lvl" Opacity="0.6"/>
                                    <TextBlock Text="{Binding AmpLevel.Value}"/>
                                    <TextBlock Text="Pan" Opacity="0.6"/>
                                    <TextBlock Text="{Binding PanLabel}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Filter" Opacity="0.6"/>
                                    <TextBlock Text="{Binding FilterSummary}"/>
                                </StackPanel>
                                <!-- Preview thumbnails (non-interactive). Row 1: filter shape. -->
                                <controls:FilterCurveControl Height="40" Preview="True" IsHitTestVisible="False" Opacity="0.85"
                                    Mode="{Binding FilterMode.Value}" Steep="{Binding FilterSlopeSteep.Value}"
                                    Cutoff="{Binding FilterCutoff.Value}" Resonance="{Binding FilterResonance.Value}"
                                    LineBrush="{StaticResource SnFilterEnvelopeBrush}" FillBrush="{StaticResource SnFilterEnvelopeFillBrush}"
                                    BackgroundBrush="{StaticResource SnEnvelopeBackgroundBrush}" GridBrush="{StaticResource SnEnvelopeGridBrush}"
                                    AxisBrush="{StaticResource SnEnvelopeAxisBrush}"/>
                                <TextBlock Text="filter shape (preview)" FontSize="10" Opacity="0.45" HorizontalAlignment="Center"/>
                                <!-- Pitch envelope preview (purple), consistent with the PCM Synth cards. -->
                                <controls:PitchEnvelopeControl Height="40" Preview="True" IsHitTestVisible="False" Opacity="0.85"
                                    Attack="{Binding PitchEnvAttack.Value}" Decay="{Binding PitchEnvDecay.Value}" Depth="{Binding PitchEnvDepth.Value}"
                                    LineBrush="{StaticResource SnPitchEnvelopeBrush}" FillBrush="{StaticResource SnPitchEnvelopeFillBrush}"
                                    BackgroundBrush="{StaticResource SnEnvelopeBackgroundBrush}" GridBrush="{StaticResource SnEnvelopeGridBrush}"
                                    AxisBrush="{StaticResource SnEnvelopeAxisBrush}"/>
                                <TextBlock Text="pitch env (preview)" FontSize="10" Opacity="0.45" HorizontalAlignment="Center"/>
                                <!-- Row 2: amp + filter envelopes overlaid on shared axes. -->
                                <controls:DualAdsrEnvelopeControl Height="44" Preview="True" IsHitTestVisible="False" Opacity="0.85"
                                    AmpAttack="{Binding AmpEnvAttack.Value}" AmpDecay="{Binding AmpEnvDecay.Value}"
                                    AmpSustain="{Binding AmpEnvSustain.Value}" AmpRelease="{Binding AmpEnvRelease.Value}"
                                    FilterAttack="{Binding FilterEnvAttack.Value}" FilterDecay="{Binding FilterEnvDecay.Value}"
                                    FilterSustain="{Binding FilterEnvSustain.Value}" FilterRelease="{Binding FilterEnvRelease.Value}"
                                    AmpLineBrush="{StaticResource SnAmpEnvelopeBrush}" AmpFillBrush="{StaticResource SnAmpEnvelopeFillBrush}"
                                    FilterLineBrush="{StaticResource SnFilterEnvelopeBrush}" FilterFillBrush="{StaticResource SnFilterEnvelopeFillBrush}"
                                    BackgroundBrush="{StaticResource SnEnvelopeBackgroundBrush}" GridBrush="{StaticResource SnEnvelopeGridBrush}"
                                    AxisBrush="{StaticResource SnEnvelopeAxisBrush}"/>
                                <TextBlock Text="amp (blue) + filter (amber) env (preview)" FontSize="10" Opacity="0.45"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            </Grid>

            <!-- Right column: Sound / Motion sub-tabs; Copy/Paste/Init shared below. -->
            <Grid Grid.Column="2" RowDefinitions="*,Auto">
                <TabControl Grid.Row="0">
                    <TabItem Header="Sound">
                        <ScrollViewer Name="SoundScroll"
                                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <!-- MinHeight = viewport so the envelope row (*) fills a tall window. -->
                            <Grid DataContext="{Binding SelectedPartial}" x:DataType="vm:SNSPartialViewModel"
                                  MinHeight="{Binding #SoundScroll.Bounds.Height}"
                                  RowDefinitions="Auto,Auto,Auto,Auto,*">

                <!-- Oscillator -->
                <Border Grid.Row="0" Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,12">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Oscillator" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <StackPanel Spacing="1">
                                <TextBlock Text="Waveform" Classes="sliderLabel" ToolTip.Tip="OSC Wave"/>
                                <ComboBox ItemsSource="{Binding OscWave.Options}"
                                          SelectedItem="{Binding OscWave.Value, Mode=TwoWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="1">
                                <TextBlock Text="Variation" Classes="sliderLabel"/>
                                <ComboBox ItemsSource="{Binding OscWaveVariation.Options}"
                                          SelectedItem="{Binding OscWaveVariation.Value, Mode=TwoWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="Pitch (semitones)" Classes="sliderLabel" ToolTip.Tip="OSC Pitch"/>
                                <controls:RotaryKnob Minimum="-24" Maximum="24" Value="{Binding OscPitch.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobCommonBrush}"/>
                            </StackPanel>
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="Detune" Classes="sliderLabel" ToolTip.Tip="OSC Detune (Flat / Center / Sharp)"/>
                                <controls:RotaryKnob Minimum="-50" Maximum="50" Value="{Binding OscDetune.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchDriftBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="6" IsVisible="{Binding ShowsPulseWidth}">
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="Pulse Width" Classes="sliderLabel" ToolTip.Tip="OSC Pulse Width"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding OscPulseWidth.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobCommonBrush}"/>
                            </StackPanel>
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="PW Shift" Classes="sliderLabel" ToolTip.Tip="OSC Pulse Width shift"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding OscPulseWidthShift.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobCommonBrush}"/>
                            </StackPanel>
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="PWM Depth" Classes="sliderLabel" ToolTip.Tip="OSC Pulse Width Mod Depth"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding OscPwmDepth.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobLfoBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="1" Width="200" IsVisible="{Binding ShowsSuperSawDetune}">
                            <TextBlock Text="Super Saw Detune" Classes="sliderLabel" ToolTip.Tip="Super Saw Detune"/>
                            <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding SuperSawDetune.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchDriftBrush}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="6" IsVisible="{Binding ShowsPcm}">
                            <StackPanel Spacing="1">
                                <TextBlock Text="PCM Wave" Classes="sliderLabel" ToolTip.Tip="Wave Number"/>
                                <ComboBox MaxDropDownHeight="300" Width="220"
                                          ItemsSource="{Binding WaveNumber.Options}"
                                          SelectedItem="{Binding WaveNumber.Value, Mode=TwoWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="1">
                                <TextBlock Text="PCM Gain (dB)" Classes="sliderLabel" ToolTip.Tip="Wave Gain"/>
                                <ComboBox ItemsSource="{Binding WaveGain.Options}"
                                          SelectedItem="{Binding WaveGain.Value, Mode=TwoWay}"/>
                            </StackPanel>
                        </StackPanel>

                        <Button HorizontalAlignment="Left" Content="Advanced oscillator parameters…"
                                Command="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).AdvancedOscillatorCommand}"/>
                    </StackPanel>
                </Border>

                <!-- Pitch -->
                <Border Grid.Row="1" Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,12">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Pitch" FontWeight="Bold"/>
                        <DockPanel>
                            <!-- Tone-wide pitch controls (apply to the whole tone, not just this partial) -->
                            <StackPanel DockPanel.Dock="Right" Spacing="1" Margin="16,0,0,0">
                                <TextBlock Text="Octave / Bend (whole tone)" Opacity="0.7"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <StackPanel Spacing="1">
                                        <TextBlock Text="Octave" ToolTip.Tip="Octave Shift"/>
                                        <controls:RotaryKnob Minimum="-3" Maximum="3"
                                                       Value="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).Header.OctaveShift.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchDriftBrush}"/>
                                    </StackPanel>
                                    <StackPanel Spacing="1">
                                        <TextBlock Text="Bend Up" ToolTip.Tip="Pitch Bend Range Up"/>
                                        <controls:RotaryKnob Minimum="0" Maximum="24"
                                                       Value="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).Header.PitchBendUp.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchBrush}"/>
                                    </StackPanel>
                                    <StackPanel Spacing="1">
                                        <TextBlock Text="Bend Down" ToolTip.Tip="Pitch Bend Range Down"/>
                                        <controls:RotaryKnob Minimum="0" Maximum="24"
                                                       Value="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).Header.PitchBendDown.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                            <!-- Per-partial pitch envelope (bipolar: up = sharp, down = flat) -->
                            <StackPanel Spacing="4">
                                <TextBlock Text="Pitch envelope (up = sharp, down = flat)"/>
                                <controls:PitchEnvelopeControl Height="120"
                                    Attack="{Binding PitchEnvAttack.Value, Mode=TwoWay}"
                                    Decay="{Binding PitchEnvDecay.Value, Mode=TwoWay}"
                                    Depth="{Binding PitchEnvDepth.Value, Mode=TwoWay}"
                                    LineBrush="{StaticResource SnPitchEnvelopeBrush}"
                                    FillBrush="{StaticResource SnPitchEnvelopeFillBrush}"
                                    BackgroundBrush="{StaticResource SnEnvelopeBackgroundBrush}"
                                    GridBrush="{StaticResource SnEnvelopeGridBrush}"
                                    AxisBrush="{StaticResource SnEnvelopeAxisBrush}"
                                    HandleBrush="{StaticResource SnEnvelopeHandleBrush}"
                                    FocusBrush="{StaticResource SnEnvelopeFocusBrush}"/>
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <StackPanel Spacing="1">
                                        <TextBlock Text="Attack" ToolTip.Tip="OSC Pitch Env Attack Time"/>
                                        <controls:RotaryKnob Minimum="0" Maximum="127"
                                                       Value="{Binding PitchEnvAttack.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchBrush}"/>
                                    </StackPanel>
                                    <StackPanel Spacing="1">
                                        <TextBlock Text="Decay" ToolTip.Tip="OSC Pitch Env Decay"/>
                                        <controls:RotaryKnob Minimum="0" Maximum="127"
                                                       Value="{Binding PitchEnvDecay.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchBrush}"/>
                                    </StackPanel>
                                    <StackPanel Spacing="1">
                                        <TextBlock Text="Depth" ToolTip.Tip="OSC Pitch Env Depth"/>
                                        <controls:RotaryKnob Minimum="-63" Maximum="63"
                                                       Value="{Binding PitchEnvDepth.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPitchBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <!-- Filter -->
                <Border Grid.Row="2" Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,12">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Filter" FontWeight="Bold"/>
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Spacing="1" Margin="16,0,0,0">
                                <TextBlock Text="Slope" ToolTip.Tip="Filter Slope"/>
                                <ToggleSwitch OnContent="Steep 24 dB" OffContent="Gentle 12 dB"
                                              IsChecked="{Binding FilterSlopeSteep.Value, Mode=TwoWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="1">
                                <TextBlock Text="Mode" ToolTip.Tip="Filter Mode"/>
                                <ListBox ItemsSource="{Binding FilterMode.Options}"
                                         SelectedItem="{Binding FilterMode.Value, Mode=TwoWay}"
                                         SelectionMode="Single" Background="Transparent" Padding="0">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate><WrapPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                </ListBox>
                            </StackPanel>
                        </DockPanel>

                        <!-- Interactive filter response: drag = Cutoff (X) / Resonance (Y); Mode/Slope above redraw it. -->
                        <controls:FilterCurveControl Height="150"
                            Mode="{Binding FilterMode.Value}"
                            Steep="{Binding FilterSlopeSteep.Value}"
                            Cutoff="{Binding FilterCutoff.Value, Mode=TwoWay}"
                            Resonance="{Binding FilterResonance.Value, Mode=TwoWay}"
                            LineBrush="{StaticResource SnFilterEnvelopeBrush}"
                            FillBrush="{StaticResource SnFilterEnvelopeFillBrush}"
                            BackgroundBrush="{StaticResource SnEnvelopeBackgroundBrush}"
                            GridBrush="{StaticResource SnEnvelopeGridBrush}"
                            AxisBrush="{StaticResource SnEnvelopeAxisBrush}"
                            HandleBrush="{StaticResource SnEnvelopeHandleBrush}"/>

                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <StackPanel Spacing="1">
                                <TextBlock Text="Cutoff (Brightness)" ToolTip.Tip="Filter Cutoff"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127"
                                               Value="{Binding FilterCutoff.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                            </StackPanel>
                            <StackPanel Spacing="1">
                                <TextBlock Text="Resonance" ToolTip.Tip="Filter Resonance"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127"
                                               Value="{Binding FilterResonance.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <StackPanel Spacing="1" Width="150">
                                <TextBlock Text="HPF Cutoff" Classes="sliderLabel" ToolTip.Tip="HPF Cutoff"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding HpfCutoff.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <Button HorizontalAlignment="Left" Content="Advanced filter parameters…"
                                Command="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).AdvancedOscillatorCommand}"/>
                    </StackPanel>
                </Border>

                <!-- Amp -->
                <Border Grid.Row="3" Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,12">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Amp" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="Level" Classes="sliderLabel" ToolTip.Tip="AMP Level"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding AmpLevel.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                            </StackPanel>
                            <StackPanel Spacing="1" Width="160">
                                <TextBlock Text="Pan (L / C / R)" Classes="sliderLabel" ToolTip.Tip="AMP Pan"/>
                                <controls:RotaryKnob Minimum="-64" Maximum="63" Value="{Binding AmpPan.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobPanBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <Button HorizontalAlignment="Left" Content="Advanced amp parameters…"
                                Command="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).AdvancedAmpCommand}"/>
                    </StackPanel>
                </Border>

                <!-- Combined Amp + Filter envelope (shared axes), fills remaining height -->
                <Border Grid.Row="4" Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6" Padding="10">
                    <Grid RowDefinitions="Auto,*,Auto">
                        <DockPanel Grid.Row="0">
                            <TextBlock Text="Envelopes — Amp + Filter (shared time / level)" FontWeight="Bold"/>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="Filter Depth" VerticalAlignment="Center" ToolTip.Tip="Filter Env Depth"/>
                                <controls:RotaryKnob Minimum="-63" Maximum="63"
                                        Value="{Binding FilterEnvDepth.Value, Mode=TwoWay}" VerticalAlignment="Center" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                <ComboBox SelectedIndex="{Binding ActiveEnvelope, Mode=TwoWay}" VerticalAlignment="Center"
                                          ToolTip.Tip="Which envelope the keyboard edits">
                                    <ComboBoxItem>Amp</ComboBoxItem>
                                    <ComboBoxItem>Filter</ComboBoxItem>
                                </ComboBox>
                            </StackPanel>
                        </DockPanel>
                        <controls:DualAdsrEnvelopeControl Grid.Row="1" MinHeight="180" Margin="0,8"
                            ActiveEnvelope="{Binding ActiveEnvelope, Mode=TwoWay}"
                            AmpAttack="{Binding AmpEnvAttack.Value, Mode=TwoWay}"
                            AmpDecay="{Binding AmpEnvDecay.Value, Mode=TwoWay}"
                            AmpSustain="{Binding AmpEnvSustain.Value, Mode=TwoWay}"
                            AmpRelease="{Binding AmpEnvRelease.Value, Mode=TwoWay}"
                            FilterAttack="{Binding FilterEnvAttack.Value, Mode=TwoWay}"
                            FilterDecay="{Binding FilterEnvDecay.Value, Mode=TwoWay}"
                            FilterSustain="{Binding FilterEnvSustain.Value, Mode=TwoWay}"
                            FilterRelease="{Binding FilterEnvRelease.Value, Mode=TwoWay}"
                            AmpLineBrush="{StaticResource SnAmpEnvelopeBrush}"
                            AmpFillBrush="{StaticResource SnAmpEnvelopeFillBrush}"
                            FilterLineBrush="{StaticResource SnFilterEnvelopeBrush}"
                            FilterFillBrush="{StaticResource SnFilterEnvelopeFillBrush}"
                            BackgroundBrush="{StaticResource SnEnvelopeBackgroundBrush}"
                            GridBrush="{StaticResource SnEnvelopeGridBrush}"
                            AxisBrush="{StaticResource SnEnvelopeAxisBrush}"
                            HandleBrush="{StaticResource SnEnvelopeHandleBrush}"
                            FocusBrush="{StaticResource SnEnvelopeFocusBrush}"/>
                        <Grid Grid.Row="2" ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Amp A/D/S/R" Foreground="{StaticResource SnAmpEnvelopeBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,2"/>
                            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,2">
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding AmpEnvAttack.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding AmpEnvDecay.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding AmpEnvSustain.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding AmpEnvRelease.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                            </StackPanel>
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Filter A/D/S/R" Foreground="{StaticResource SnFilterEnvelopeBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding FilterEnvAttack.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding FilterEnvDecay.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding FilterEnvSustain.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                <controls:RotaryKnob Minimum="0" Maximum="127" Value="{Binding FilterEnvRelease.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                            </Grid>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Motion">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <StackPanel DataContext="{Binding SelectedPartial}" x:DataType="vm:SNSPartialViewModel"
                                        Spacing="6" Margin="0,0,0,8">
                                <ContentControl Content="{Binding AutomaticMotion}"/>
                                <ContentControl Content="{Binding ModWheelMotion}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="FX">
                        <!-- Horizontal-disabled so the MFX param WrapPanel grid wraps (vertical scroll)
                             instead of running off the right edge. -->
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                            <ContentControl Content="{Binding Mfx}" Margin="0,8,0,0"/>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Response">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <Border DataContext="{Binding SelectedPartial}" x:DataType="vm:SNSPartialViewModel"
                                    Background="{StaticResource SnPanelBackgroundBrush}" CornerRadius="6"
                                    Padding="10" Margin="0,8,0,0" VerticalAlignment="Top">
                                <StackPanel Spacing="6">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <TextBlock Text="Response" FontWeight="Bold"/>
                                        <TextBlock Text="— how this partial reacts to playing"
                                                   Opacity="0.6" VerticalAlignment="Bottom"/>
                                    </StackPanel>
                                    <Grid ColumnDefinitions="130,210,210" RowDefinitions="Auto,Auto,Auto,Auto"
                                          ColumnSpacing="6" RowSpacing="10">
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="→ Brightness (filter)" FontWeight="Bold"/>
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="→ Loudness (amp)" FontWeight="Bold"/>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Velocity" VerticalAlignment="Center"
                                                   ToolTip.Tip="How hard you play"/>
                                        <controls:RotaryKnob Grid.Row="1" Grid.Column="1" Minimum="-63" Maximum="63"
                                                ToolTip.Tip="Filter Env Velocity Sens"
                                                Value="{Binding FilterEnvVeloSens.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                        <controls:RotaryKnob Grid.Row="1" Grid.Column="2" Minimum="-63" Maximum="63"
                                                ToolTip.Tip="AMP Level Velocity Sens"
                                                Value="{Binding AmpVeloSens.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>

                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Aftertouch" VerticalAlignment="Center"
                                                   ToolTip.Tip="Pressure after the key is down"/>
                                        <controls:RotaryKnob Grid.Row="2" Grid.Column="1" Minimum="-63" Maximum="63"
                                                ToolTip.Tip="Cutoff Aftertouch Sens"
                                                Value="{Binding CutoffAftertouchSens.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                        <controls:RotaryKnob Grid.Row="2" Grid.Column="2" Minimum="-63" Maximum="63"
                                                ToolTip.Tip="Level Aftertouch Sens"
                                                Value="{Binding LevelAftertouchSens.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>

                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Key position" VerticalAlignment="Center"
                                                   ToolTip.Tip="Higher vs lower notes (keyfollow)"/>
                                        <controls:RotaryKnob Grid.Row="3" Grid.Column="1" Minimum="-100" Maximum="100"
                                                ToolTip.Tip="Filter Cutoff Keyfollow"
                                                Value="{Binding FilterCutoffKeyfollow.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobFilterBrush}"/>
                                        <controls:RotaryKnob Grid.Row="3" Grid.Column="2" Minimum="-100" Maximum="100"
                                                ToolTip.Tip="AMP Level Keyfollow"
                                                Value="{Binding AmpKeyfollow.Value, Mode=TwoWay}" AccentBrush="{StaticResource KnobAmpBrush}"/>
                                    </Grid>
                                    <Button HorizontalAlignment="Left" Content="Advanced partial parameters…"
                                            Command="{Binding #Root.((vm:SNSynthToneEditorViewModel)DataContext).AdvancedOscillatorCommand}"/>
                                </StackPanel>
                            </Border>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                    <Button Content="Copy partial" Command="{Binding CopyPartialCommand}"/>
                    <Button Content="Paste partial" Command="{Binding PastePartialCommand}"/>
                    <Button Content="Init partial" Command="{Binding InitPartialCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
